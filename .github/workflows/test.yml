name: Test

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: "go.mod"

      - name: golangci-lint
        uses: golangci/golangci-lint-action@ba0d7d2ec06a0ea1cb5fa41b2e4a3ab91d21278a # v9.3.0
        with:
          version: latest
          args: --timeout=5m

      - name: Acceptance (lock, build, tests, example check and generation)
        run: make verify

      - name: Check schema.json is up to date
        run: |
          go run ./cmd/mdschema schema -o schema.json.tmp
          diff schema.json schema.json.tmp || (echo "schema.json is out of sync. Run 'go run ./cmd/mdschema schema -o schema.json' and commit." && exit 1)
